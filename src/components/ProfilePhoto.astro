<a
  href="/profile1.png"
  target="_blank"
  rel="noopener noreferrer"
  aria-label="Open Bagas profile photo in a new tab"
  class="photo-wrapper"
>
  <span class="photo-ring">
    <img
      src="/profile1.png"
      alt="Bagas"
      class="photo-img"
      width="280"
      height="280"
    />
  </span>
  <span class="photo-glow"></span>
</a>

<style>
  .photo-wrapper {
    position: relative;
    display: flex;
    width: min(280px, 100%);
    align-items: center;
    justify-content: center;
  }

  .photo-ring {
    position: relative;
    z-index: 1;
    display: block;
    width: 100%;
    padding: 3px;
    border-radius: 1.25rem;
    background: linear-gradient(135deg, var(--eva-panel), var(--eva-green));
  }

  .photo-img {
    display: block;
    width: 100%;
    height: auto;
    aspect-ratio: 1;
    border-radius: 1rem;
    object-fit: cover;
    object-position: top center;
    background-color: var(--eva-void);
  }

  .photo-glow {
    position: absolute;
    inset: -24px;
    z-index: 0;
    border-radius: 50%;
    background: radial-gradient(
      ellipse at center,
      var(--eva-orange) 0%,
      transparent 70%
    );
    opacity: 0.12;
    filter: blur(24px);
    animation: pulse-glow 4s ease-in-out infinite;
  }

  @keyframes pulse-glow {
    0%,
    100% {
      opacity: 0.15;
      transform: scale(1);
    }
    50% {
      opacity: 0.3;
      transform: scale(1.08);
    }
  }
</style>
